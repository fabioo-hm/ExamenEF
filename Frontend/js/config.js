// Si tu backend usa HTTP (lo más común con dotnet run)
export const API_URL = "http://localhost:5104/api";

// ⚠️ Si tu backend realmente corre con HTTPS (verifica la consola de .NET)
 // export const API_URL = "https://localhost:5104/api";
